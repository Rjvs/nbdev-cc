[project]
name = "nbdev-mcp"
version = "0.1.0"
description = "MCP server and plugin for nbdev notebook development with Claude Code"
requires-python = ">=3.10"
license = "Apache-2.0"
readme = "README.md"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Text Editors :: Integrated Development Environments (IDE)",
]
dependencies = [
    "mcp>=1.0",
]

[project.urls]
Homepage = "https://github.com/AnswerDotAI/nbdev-cc"
Repository = "https://github.com/AnswerDotAI/nbdev-cc"
Issues = "https://github.com/AnswerDotAI/nbdev-cc/issues"

[project.scripts]
nbdev-mcp = "nbdev_mcp.cli:main"

[dependency-groups]
dev = [
    "pytest>=7.0",
    "nbformat>=5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nbdev_mcp"]
